# fancyCat

## Command Description

`fancyCat` is a simple Node.js command-line tool that combines the `cat` and `wc` commands.

It displays the contents of a file and then shows the total number of lines in that file.

### How to Run It

```bash
node fancyCat.js practice.txt
```
### Commands Combined
_ `cat` - displays the contents of a file
_`wc` - counts information in a file, such as the number of lines

## AI-Assisted Programming

I used AI to help me understand the cat and wc commands and how they could be combined into one simple Node.js command-line tool.

AI also helped me come up with test scenarios and identify edge cases, such as running the program without a filename or using a filename that does not exist.

I still had to test the program myself, review the results, and decide what changes to make to the code.

One thing AI got wrong at first was misunderstanding which command I wanted to build.
